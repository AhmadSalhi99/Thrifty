<!DOCTYPE html>
<html lang="en">
<head>
    <title>@ViewData["Title"] </title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!--===============================================================================================-->
    <link href="~/css/dashbord/bootstrap.min.css" rel="stylesheet" />
    <link rel="stylesheet" type="text/css" href="~/css/Account/util.css">
    <link rel="stylesheet" type="text/css" href="~/css/Account/main.css">
    <!--===============================================================================================-->
    <link rel="stylesheet" href="~/css/styles.css">
    <style>
        .text-danger {
            padding-inline-end: 10px;
            font-size: 24px;
            float: right;
            position: absolute;
            right: 0;
            top: 2px;
        }

        .input100:not(:placeholder-shown) {
            padding-top: 29px !important;
            font-size: 15px !important;
        }

            .input100:not(:placeholder-shown) ~ .label-input100 {
                top: 6px !important;
                font-size: 13px !important;
            }

        .alter-custome {
            position: absolute;
            top: 50px;
            left: 50%;
            transform: translateX(-50%);
        }
        .login100-form {
            background-color: var(--body-color);
        }

        .login100-form-title {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            color: var(--title-color);
        }

        .label-input100, .txt1, .input100, input::placeholder, #cityId {
            color: var(--title-color) !important;
        }
    </style>
</head>
<body style="background-color: #666666;">
    <div class="limiter">
        <div class="container-login100">
            <div class="wrap-login100">
                @RenderBody()
                <div class="login100-more" style="background-image: url('../Img/Account/Account-Background.jpg'); filter: grayscale(100%);">
                </div>
            </div>
        </div>
    </div>

    <!--===============================================================================================-->
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <!--===============================================================================================-->
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
</body>
</html>

<script>
    //const themeButton = document.getElementById('theme-button')
    const darkTheme = 'dark-theme'
    const iconTheme = 'ri-sun-line'
    var logo = document.getElementById("nav-logo")

    const selectedTheme = localStorage.getItem('selected-theme')
    const selectedIcon = localStorage.getItem('selected-icon')

    const getCurrentTheme = () => document.body.classList.contains(darkTheme) ? 'dark' : 'light'
    //const getCurrentIcon = () => themeButton.classList.contains(iconTheme) ? 'ri-moon-line' : 'ri-sun-line'

    if (selectedTheme) {
        document.body.classList[selectedTheme === 'dark' ? 'add' : 'remove'](darkTheme)
        //themeButton.classList[selectedIcon === 'ri-moon-line' ? 'add' : 'remove'](iconTheme)

    }
    if (selectedTheme === "light") {
        logo.classList.add("light")
        logo.classList.remove("dark")
        logo.src = "/img/logo.png";
    } else {
        logo.classList.remove("light");
        logo.classList.add("dark");
        logo.src = "/img/logo_dark.png";
    }

</script>